{
  "entities": {
    "userProfile": {
      "title": "User Profile",
      "description": "Represents a user's public profile.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        }
      },
      "required": [
        "displayName",
        "email"
      ]
    },
    "product": {
      "title": "Product",
      "description": "Represents a product in the store, which can be a cat or a pet product.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the product."
        },
        "type": {
          "type": "string",
          "enum": [
            "cat",
            "pet_product"
          ],
          "description": "The type of product."
        },
        "category": {
          "type": "string",
          "enum": [
            "food",
            "toys",
            "litter",
            "accessories",
            "siamese",
            "persian",
            "sphynx"
          ],
          "description": "The category of the product."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the product."
        },
        "price": {
          "type": "number",
          "description": "The price of the product."
        },
        "currency": {
          "type": "string",
          "description": "The currency of the price (e.g., PKR)."
        },
        "stockQuantity": {
          "type": "integer",
          "description": "The quantity of the product in stock."
        },
        "mainImageUrl": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the main product image."
        },
        "galleryImageUrls": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "uri"
          },
          "description": "A list of URLs for gallery images."
        },
        "isFeatured": {
          "type": "boolean",
          "description": "Whether the product is featured on the homepage."
        },
        "isListed": {
          "type": "boolean",
          "description": "Whether the product is listed and visible in the shop."
        },
        "ratingAverage": {
          "type": "number",
          "description": "The average rating of the product."
        },
        "ratingCount": {
          "type": "integer",
          "description": "The number of ratings the product has received."
        }
      },
      "required": [
        "name",
        "type",
        "category",
        "description",
        "price",
        "currency",
        "stockQuantity",
        "mainImageUrl"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com",
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/userProfile"
      },
      "description": "Stores public user profile data."
    },
    "/products/{productId}": {
      "schema": {
        "$ref": "#/entities/product"
      },
      "description": "Stores all product data for the e-commerce store."
    }
  }
}
